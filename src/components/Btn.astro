---
const { href, className, name, bgColor, textColor } = Astro.props;
---

<button type="button" class={className} data-link={href}>
  {name}
</button>

<style define:vars={{ bgColor, textColor }}>
  button {
    min-width: 175px;
    display: block;
    position: relative;
    text-align: center;
    width: max-content;
    text-decoration: none;
    color: var(--textColor) !important;
    text-transform: uppercase;
    padding: 10px;
    font-size: 14px;
    border: 1px solid var(--bgColor);
    z-index: 1;
    transition:
      color 0.45s cubic-bezier(0.785, 0.135, 0.15, 0.86),
      border 0.45s cubic-bezier(0.785, 0.135, 0.15, 0.86);
    cursor: pointer;
    line-height: normal;
    background: transparent;

    &::before {
      background-color: var(--bgColor);
      position: absolute;
      content: "";
      display: block;
      left: 0;
      top: 0;
      right: 0;
      bottom: 0;
      width: 100%;
      height: 100%;
      transform: scale(1);
      transform-origin: left center;
      z-index: -1;
      transition: transform 0.45s cubic-bezier(0.785, 0.135, 0.15, 0.86);
    }

    &:hover {
      color: var(--bgColor) !important;
      background: transparent;
      &::before {
        transform-origin: right center;
        transform: scaleX(0);
      }
    }
  }
</style>

<script>
  $("button").on("click", function () {
    console.log("Button clicked");

    // You can perform any action you need here, like accessing a data attribute
    const dataLink = $(this).data("link"); // If your button has a data-link attribute
  });
</script>
